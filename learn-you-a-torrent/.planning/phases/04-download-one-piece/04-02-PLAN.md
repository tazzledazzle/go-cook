---
phase: 04-download-one-piece
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - internal/pieces/piece.go
  - internal/pieces/piece_test.go
  - internal/torrent/torrent.go
autonomous: true
requirements: [PIEC-03, PIEC-04]
must_haves:
  truths:
    - "Validate returns true when SHA1 matches expected hash"
    - "Validate returns false on corrupt data"
    - "Reset clears buffer and block tracking for re-download"
---

<objective>
TDD: SHA1 piece verification and reset on mismatch (Pitfall 7).

Purpose: PIEC-03 validate + PIEC-04 failed hash resets piece.
Output: Piece.Validate, Piece.Reset; optional Info.PieceHash(index).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-download-one-piece/04-01-PLAN.md
@.planning/phases/04-download-one-piece/04-RESEARCH.md (golden hash)
@testdata/README.md
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Piece validation and reset tests</name>
  <files>internal/pieces/piece_test.go</files>
  <action>
    Add tests ONLY (piece.go from 04-01 must exist):
    TestPieceValidate_matchesGoldenHash:
    - Build full 16384-byte buffer: "hello world\n" + zero padding
    - expected hash 98aaed442721e0cecdd9bf7b8cbb3e1ff1b1536a
    - Validate(expected) true
    TestPieceValidate_rejectsCorruptData:
    - Same buffer but flip one byte → Validate false
    TestPieceReset_clearsState:
    - WriteBlock, Validate fails, Reset(), Complete() false, Bytes all zero
    Optional: TestInfoPieceHash from parsed minimal.torrent if adding torrent.Info.PieceHash helper.
    Do NOT add Validate/Reset impl yet — extend tests to call undefined methods (fail compile).
  </action>
  <verify>
    <automated>go test ./internal/pieces/... -run TestPieceValidate -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Validate and Reset</name>
  <files>internal/pieces/piece.go, internal/torrent/torrent.go</files>
  <action>
    Piece.Validate(expected [20]byte) bool — crypto/sha1.Sum(p.data), bytes.Equal.
    Piece.Reset() — zero data, clear received map.
    Add Info.PieceHash(index int) ([20]byte, error) on torrent.Info — slice Pieces at index*20.
  </action>
  <verify>
    <automated>go test ./internal/pieces/... -run 'TestPieceValidate|TestPiece' -v</automated>
  </verify>
  <done>GREEN verified — PIEC-03, PIEC-04 complete.</done>
</task>

</tasks>

<output>
Create 04-02-SUMMARY.md
</output>
