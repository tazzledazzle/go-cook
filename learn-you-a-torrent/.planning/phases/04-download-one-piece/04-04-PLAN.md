---
phase: 04-download-one-piece
plan: 04
type: tdd
wave: 4
depends_on: [03-03]
files_modified:
  - internal/peer/message.go
  - internal/peer/message_test.go
  - internal/peer/connection.go
  - internal/peer/connection_test.go
autonomous: true
requirements: [PEER-04]
must_haves:
  truths:
    - "BuildRequest encodes index, begin, length as 12-byte BE payload"
    - "ParsePiece extracts index, begin, block from piece message"
    - "Connection.SendInterested and SendRequest write correct messages"
---

<objective>
TDD: Request/Piece wire encoding and Connection send helpers (Pitfalls 5–6).

Purpose: PEER-04 wire layer — Interested, Request, Piece parse.
Output: BuildRequest, ParsePiece, ParseRequest; Connection send methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-download-one-piece/04-RESEARCH.md
@internal/peer/message.go
@internal/peer/connection.go
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Request and Piece message tests</name>
  <files>internal/peer/message_test.go, internal/peer/connection_test.go</files>
  <action>
    Add to message_test.go:
    TestBuildRequest_roundtrip: BuildRequest(0, 0, 12) → WriteMessage → ReadMessage → ParseRequest equals inputs.
    TestParsePiece_extractsBlock: build piece payload index=0, begin=0, block="hello world\n" → ParsePiece returns correct fields.
    Add to connection_test.go:
    TestConnection_sendInterested: net.Pipe, goroutine ReadMessage → MsgInterested.
    TestConnection_sendRequest: ReadMessage → MsgRequest with expected payload.
    No new implementations — fail compile.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run 'TestBuildRequest|TestParsePiece|TestConnection_send' -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Request/Piece helpers and Connection sends</name>
  <files>internal/peer/message.go, internal/peer/connection.go</files>
  <action>
    BuildRequest(index, begin, length uint32) Message — MsgRequest, 12-byte BE payload.
    ParseRequest(msg Message) (index, begin, length uint32, err error)
    ParsePiece(msg Message) (index, begin uint32, block []byte, err error)
    Connection.SendInterested() — WriteMessage(MsgInterested)
    Connection.SendRequest(index, begin, length uint32) — WriteMessage(BuildRequest(...))
    Connection.WriteMessage(msg Message) — delegate to peer.WriteMessage(c.conn, msg)
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run 'TestBuildRequest|TestParsePiece|TestConnection_send' -v</automated>
  </verify>
  <done>GREEN verified — PEER-04 wire complete.</done>
</task>

</tasks>

<output>
Create 04-04-SUMMARY.md
</output>
