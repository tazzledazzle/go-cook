---
phase: 04-download-one-piece
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/pieces/piece.go
  - internal/pieces/piece_test.go
autonomous: true
requirements: [PIEC-01, PIEC-02]
must_haves:
  truths:
    - "WriteBlock copies data at begin offset within piece buffer"
    - "Complete returns true only when all expected byte ranges received"
    - "Bytes returns full piece-length buffer"
---

<objective>
TDD: piece buffer assembly — track blocks at correct begin offsets (Pitfall 7 prevention).

Purpose: PIEC-01 state tracking + PIEC-02 block placement.
Output: `Piece` type with NewPiece, WriteBlock, Complete, Bytes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-download-one-piece/04-CONTEXT.md
@.planning/phases/04-download-one-piece/04-RESEARCH.md
@.planning/research/PITFALLS.md (Pitfall 6, 7)
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Piece buffer assembly tests</name>
  <files>internal/pieces/piece_test.go</files>
  <action>
    Create piece_test.go ONLY. Package `pieces`.
    TestPiece_writeBlockAtBegin: NewPiece(16384), WriteBlock(0, []byte("hello world\n")), Bytes()[0:12] matches, Complete() false for partial (only 12 of 16384 if tracking full piece — OR Complete when single block covers entire logical piece for minimal torrent).
    For Phase 4 minimal slice: use `NewPiece(16384)` with `SetExpectedSize(12)` OR track blocks explicitly:
    - TestPiece_writeBlockAtOffset: data at begin=0 lands at buffer[0]
    - TestPiece_writeBlockAtNonZeroBegin: data at begin=16384-n for multi-block (optional table row)
    - TestPiece_completeWhenAllBlocksReceived: mark blocks received via WriteBlock calls; Complete() true
    Use BlockSize constant 16384 in tests.
    Do NOT create piece.go — must fail compile.
  </action>
  <verify>
    <automated>go test ./internal/pieces/... -run TestPiece -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Piece buffer</name>
  <files>internal/pieces/piece.go</files>
  <action>
    const BlockSize = 1 << 14
    type Piece struct { data []byte; received map[int]bool }
    NewPiece(length int) *Piece — allocate zeroed buffer of length.
    WriteBlock(begin int, block []byte) error — bounds check, copy, mark block index (begin/BlockSize) received.
    Complete() bool — all blocks from 0 to ceil(length/BlockSize)-1 marked received.
    Bytes() []byte — return data slice.
    For length=16384 with single 12-byte block at begin=0: Complete() true after one WriteBlock (one block covers begin 0..16383 slot when length=12? Use logical: numBlocks = (length + BlockSize - 1) / BlockSize; for 16384 piece with file 12 bytes, still 1 block at begin=0 length=12).
  </action>
  <verify>
    <automated>go test ./internal/pieces/... -run TestPiece -v</automated>
  </verify>
  <done>GREEN verified — PIEC-01, PIEC-02 complete.</done>
</task>

</tasks>

<output>
Create 04-01-SUMMARY.md
</output>
