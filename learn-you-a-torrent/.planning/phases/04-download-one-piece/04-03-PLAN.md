---
phase: 04-download-one-piece
plan: 03
type: tdd
wave: 3
depends_on: []
files_modified:
  - internal/file/writer.go
  - internal/file/writer_test.go
autonomous: true
requirements: [FILE-01, FILE-02]
must_haves:
  truths:
    - "Writer creates file named from torrent info.name"
    - "WritePiece writes at index * pieceLength offset"
    - "Last piece writes only remaining file bytes (12 for minimal fixture)"
---

<objective>
TDD: verified piece bytes written to disk at correct offset.

Purpose: FILE-01 output filename + FILE-02 byte offset.
Output: `file.Writer` with NewWriter, WritePiece.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-download-one-piece/04-CONTEXT.md
@.planning/phases/04-download-one-piece/04-RESEARCH.md
@internal/torrent/torrent.go
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: File writer tests</name>
  <files>internal/file/writer_test.go</files>
  <action>
    Create writer_test.go ONLY. Package `file`. Use t.TempDir().
    TestWriter_createsNamedFile:
    - NewWriter(dir, torrent.Info{Name: "test.txt", Length: 12, PieceLength: 16384})
    - Assert file exists at dir/test.txt
    TestWriter_writePieceAtOffsetZero:
    - WritePiece(0, pieceData, info) where pieceData is 16384 bytes with "hello world\n" prefix
    - os.ReadFile → 12 bytes equal "hello world\n" (truncate to info.Length for last piece)
    Do NOT create writer.go — fail compile.
  </action>
  <verify>
    <automated>go test ./internal/file/... -run TestWriter -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement file Writer</name>
  <files>internal/file/writer.go</files>
  <action>
    type Writer struct { path string; info torrent.Info; file *os.File }
    NewWriter(dir string, info torrent.Info) (*Writer, error) — os.Create(filepath.Join(dir, info.Name))
    WritePiece(index int, data []byte) error:
    - offset := int64(index) * info.PieceLength
    - writeLen := min(info.PieceLength, info.Length - offset)
    - WriteAt(data[:writeLen], offset) or Seek+Write
    Close() error
  </action>
  <verify>
    <automated>go test ./internal/file/... -run TestWriter -v</automated>
  </verify>
  <done>GREEN verified — FILE-01, FILE-02 complete.</done>
</task>

</tasks>

<output>
Create 04-03-SUMMARY.md
</output>
