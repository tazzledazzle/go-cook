---
phase: 02-tracker-announce
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/tracker/announce.go
  - internal/tracker/announce_test.go
autonomous: true
requirements: [TRCK-01]
must_haves:
  truths:
    - "BuildAnnounceURL percent-encodes raw 20-byte info_hash (not hex)"
    - "Query includes peer_id, port, uploaded, downloaded, left, compact=1"
    - "RED phase confirmed test failed before announce.go implementation existed"
  artifacts:
    - path: "internal/tracker/announce.go"
      provides: "BuildAnnounceURL and AnnounceRequest types"
    - path: "internal/tracker/announce_test.go"
      provides: "Golden URL test with known info_hash bytes"
  key_links:
    - from: "announce_test.go"
      to: "testdata/README.md"
      via: "golden info_hash bytes"
      pattern: "90dc02db"
---

<objective>
TDD slice: build a valid HTTP tracker announce URL from torrent metadata.

Purpose: TRCK-01 — correct query encoding is the #1 tracker pitfall (binary info_hash).
Output: Pure function `BuildAnnounceURL` with golden test, no HTTP yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/tdd.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tracker-announce/02-CONTEXT.md
@.planning/phases/02-tracker-announce/02-RESEARCH.md
@.planning/research/PITFALLS.md
@testdata/README.md
</context>

<feature>
  <name>BuildAnnounceURL</name>
  <files>internal/tracker/announce.go, internal/tracker/announce_test.go</files>
  <behavior>
    AnnounceRequest{InfoHash [20]byte, PeerID [20]byte, Port uint16, Uploaded, Downloaded, Left int64}
    BuildAnnounceURL("http://example.com/announce", req) returns URL whose query contains:
    - info_hash as percent-encoded raw bytes (e.g. %90%DC%02%DB... for minimal.torrent hash)
    - compact=1
    - left=12 for minimal fixture file length
  </behavior>
  <implementation>
    Use net/url.URL and url.Values; set info_hash via string(hash[:]) so Encode percent-escapes bytes.
    Append query to announce base URL (handle base that already has query params — not needed v1).
  </implementation>
</feature>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Write failing TestBuildAnnounceURL golden test</name>
  <read_first>
    - .planning/phases/02-tracker-announce/02-RESEARCH.md
    - testdata/README.md
    - .planning/research/PITFALLS.md (Pitfall 2)
  </read_first>
  <files>internal/tracker/announce_test.go</files>
  <action>
    Create internal/tracker/announce_test.go ONLY (no announce.go yet, or empty stub that makes test fail).
    Define AnnounceRequest in test file OR reference type that does not exist yet (forces compile fail = acceptable RED).
    TestBuildAnnounceURL_minimalFixture:
    - info_hash bytes from testdata/README golden: 90dc02db9bd6d3808cbfdbba2633f4c6af7180f0
    - peer_id: 20-byte fixed test value -GO0001-xxxxxxxx (pad to 20)
    - port 6881, uploaded 0, downloaded 0, left 12
    - base URL http://example.com/announce
    Assert result contains compact=1, left=12, and info_hash query param with %90%DC%02%DB (Go uppercase escaping).
    DO NOT create announce.go implementation yet.
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... -run TestBuildAnnounceURL exits non-zero
    - Failure is compile error OR assertion failure (feature missing), NOT test typo
    - Record exact failure output in commit message or plan notes
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tracker/... -run TestBuildAnnounceURL -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified — test fails for expected reason.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement BuildAnnounceURL minimal pass</name>
  <read_first>
    - internal/tracker/announce_test.go
  </read_first>
  <files>internal/tracker/announce.go</files>
  <action>
    Create announce.go with AnnounceRequest struct and BuildAnnounceURL(base string, req AnnounceRequest) (string, error).
    Minimal implementation only — enough to pass TestBuildAnnounceURL.
    Move AnnounceRequest to announce.go if defined in test.
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... -run TestBuildAnnounceURL exits 0
    - URL contains percent-encoded binary info_hash (not 40-char hex literal)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tracker/... -run TestBuildAnnounceURL -v</automated>
  </verify>
  <done>GREEN verified — golden URL test passes.</done>
</task>

<task type="tdd" phase="refactor">
  <name>REFACTOR: Add edge-case tests, clean URL building</name>
  <read_first>
    - internal/tracker/announce.go
    - internal/tracker/announce_test.go
  </read_first>
  <files>internal/tracker/announce.go, internal/tracker/announce_test.go</files>
  <action>
    Add TestBuildAnnounceURL_rejectsEmptyBase if validation added.
    Extract query param names as constants only if duplication exists — no YAGNI.
    Ensure all tests still pass.
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... exits 0
    - No behavior change from GREEN phase
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tracker/... -v</automated>
  </verify>
  <done>REFACTOR complete (or skipped if nothing to clean).</done>
</task>

</tasks>

<verification>
go test ./internal/tracker/... -run TestBuildAnnounceURL -v
</verification>

<success_criteria>
- TRCK-01 satisfied
- RED failure observed and documented in 02-01-SUMMARY.md
- Separate commits: test (RED) then feat (GREEN) then optional refactor
</success_criteria>

<output>
Create .planning/phases/02-tracker-announce/02-01-SUMMARY.md with RED/GREEN/REFACTOR notes and commit hashes.
</output>
