---
phase: 02-tracker-announce
plan: 03
type: tdd
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - internal/tracker/client.go
  - internal/tracker/client_test.go
autonomous: true
requirements: [TRCK-03]
must_haves:
  truths:
    - "Client.Announce GETs built URL and returns parsed peers from mock tracker"
    - "Uses internal/bencode to decode response peers string"
    - "httptest server — no network in CI"
  artifacts:
    - path: "internal/tracker/client.go"
      provides: "Client with Announce method"
    - path: "internal/tracker/client_test.go"
      provides: "httptest integration test"
  key_links:
    - from: "client.go"
      to: "internal/torrent/parser.go"
      via: "ParseTorrent + Announce"
      pattern: "ParseTorrent"
    - from: "client.go"
      to: "internal/bencode/decode.go"
      via: "decode tracker response body"
      pattern: "bencode.NewDecoder"
---

<objective>
TDD slice: end-to-end tracker announce against mock HTTP server.

Purpose: TRCK-03 — wire URL builder + peer parser + HTTP + bencode response.
Output: Client.Announce(*torrent.Torrent) ([]Peer, error)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/02-tracker-announce/02-CONTEXT.md
@.planning/phases/02-tracker-announce/02-01-PLAN.md
@.planning/phases/02-tracker-announce/02-02-PLAN.md
@internal/torrent/parser.go
@internal/bencode/decode.go
@testdata/minimal.torrent
</context>

<feature>
  <name>Client.Announce</name>
  <files>internal/tracker/client.go, internal/tracker/client_test.go</files>
  <behavior>
    httptest server returns bencoded body: d6:peers6:&lt;127.0.0.1:6881 compact&gt;e
    Client loads minimal.torrent, calls Announce, returns 1 peer 127.0.0.1:6881
    Server handler validates request query contains info_hash and compact=1
  </behavior>
  <implementation>
    Client struct: HTTPClient *http.Client, PeerID [20]byte
    NewClient(peerID) with default http.Client if nil
    Announce: build URL from torrent.Announce + AnnounceRequest from torrent fields
    GET, read body, bencode decode, extract peers string, ParseCompactPeers
  </implementation>
</feature>

<tasks>

<task type="tdd" phase="red">
  <name>RED: httptest integration TestClient_Announce</name>
  <read_first>
    - internal/tracker/announce.go
    - internal/tracker/peers.go
    - testdata/minimal.torrent
    - internal/torrent/parser.go
  </read_first>
  <files>internal/tracker/client_test.go</files>
  <action>
    Write TestClient_Announce_mockTracker BEFORE client.go:
    - Start httptest.Server; handler checks r.URL.Query has compact=1 and info_hash present
    - Response body: hand-built bencode d6:peers6:&lt;127.0.0.1:6881 6 bytes&gt;e
    - Override torrent announce URL to server.URL (parse minimal.torrent, replace Announce field)
    - Client with fixed PeerID; call Announce; assert 1 peer IP 127.0.0.1 port 6881
    No client.go — test must fail (compile or runtime).
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... -run TestClient_Announce exits non-zero before implementation
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tracker/... -run TestClient_Announce -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Client.Announce</name>
  <read_first>
    - internal/tracker/client_test.go
    - internal/bencode/decode.go
  </read_first>
  <files>internal/tracker/client.go</files>
  <action>
    Implement Client and Announce method minimal pass.
    Decode response: top-level map, peers key as string, feed to ParseCompactPeers.
    Handle HTTP non-200 as error.
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... -run TestClient_Announce exits 0
    - go test ./... exits 0 (no regressions)
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -v</automated>
  </verify>
  <done>GREEN verified — Phase 2 goal complete.</done>
</task>

<task type="tdd" phase="refactor">
  <name>REFACTOR: Inject HTTPClient, extract decodeTrackerResponse</name>
  <files>internal/tracker/client.go</files>
  <action>
    Ensure HTTPClient is injectable (already for httptest).
    Extract private decodePeersResponse(body []byte) if Announce is long — only if improves clarity.
    Stay green.
  </action>
  <verify>
    <automated>go test ./... -v</automated>
  </verify>
  <done>REFACTOR complete or skipped.</done>
</task>

</tasks>

<verification>
go test ./internal/tracker/... -v
go test ./...
</verification>

<success_criteria>
- TRCK-03 satisfied
- Phase 2 ROADMAP success criteria met
- Full test suite green
</success_criteria>

<output>
Create .planning/phases/02-tracker-announce/02-03-SUMMARY.md
</output>
