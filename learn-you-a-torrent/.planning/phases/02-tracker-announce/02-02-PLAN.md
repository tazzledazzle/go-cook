---
phase: 02-tracker-announce
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - internal/tracker/peer.go
  - internal/tracker/peers.go
  - internal/tracker/peers_test.go
autonomous: true
requirements: [TRCK-02]
must_haves:
  truths:
    - "ParseCompactPeers converts 6-byte chunks to IP + port (big-endian)"
    - "Empty input returns empty slice, not error"
    - "Malformed length (not multiple of 6) returns error"
  artifacts:
    - path: "internal/tracker/peers.go"
      provides: "ParseCompactPeers"
    - path: "internal/tracker/peer.go"
      provides: "Peer struct"
  key_links:
    - from: "peers_test.go"
      to: "peers.go"
      via: "table-driven compact binary fixtures"
      pattern: "ParseCompactPeers"
---

<objective>
TDD slice: parse tracker compact peer format (6 bytes per peer).

Purpose: TRCK-02 — binary IP/port layout is easy to get wrong (endianness).
Output: Pure function ParseCompactPeers with table-driven tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/02-tracker-announce/02-RESEARCH.md
@.planning/phases/02-tracker-announce/02-01-PLAN.md
</context>

<feature>
  <name>ParseCompactPeers</name>
  <files>internal/tracker/peer.go, internal/tracker/peers.go, internal/tracker/peers_test.go</files>
  <behavior>
    127.0.0.1:6881 → []byte{127,0,0,1,0x1a,0xe1} → Peer{IP: 127.0.0.1, Port: 6881}
    Two peers: 12 bytes → two Peer structs
    Empty: nil/empty slice
    5 bytes: error
  </behavior>
</feature>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Table-driven ParseCompactPeers tests</name>
  <read_first>
    - .planning/phases/02-tracker-announce/02-RESEARCH.md (compact format)
  </read_first>
  <files>internal/tracker/peers_test.go</files>
  <action>
    Create peers_test.go with TestParseCompactPeers table:
    - single peer 127.0.0.1:6881
    - two peers (127.0.0.1:6881 + 192.168.0.1:51413)
    - empty input → empty slice, no error
    - 5 bytes → error
    Do NOT implement peers.go yet (or stub returning nil,nil).
    Run tests — must FAIL.
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... -run TestParseCompactPeers exits non-zero
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tracker/... -run TestParseCompactPeers -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Peer type and ParseCompactPeers</name>
  <read_first>
    - internal/tracker/peers_test.go
  </read_first>
  <files>internal/tracker/peer.go, internal/tracker/peers.go</files>
  <action>
    peer.go: type Peer struct { IP net.IP; Port uint16 }
    peers.go: ParseCompactPeers(data []byte) ([]Peer, error)
    Use binary.BigEndian.Uint16 for port at offset+4.
    IP: net.IP(data[i:i+4]).
  </action>
  <acceptance_criteria>
    - go test ./internal/tracker/... -run TestParseCompactPeers exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tracker/... -run TestParseCompactPeers -v</automated>
  </verify>
  <done>GREEN verified.</done>
</task>

<task type="tdd" phase="refactor">
  <name>REFACTOR: String() or Equal helper for test readability (optional)</name>
  <files>internal/tracker/peer.go</files>
  <action>
    Add Peer.String() or Address() string method ONLY if tests benefit — skip if table assertions are clear.
    All tests green.
  </action>
  <verify>
    <automated>go test ./internal/tracker/... -v</automated>
  </verify>
  <done>REFACTOR complete or skipped.</done>
</task>

</tasks>

<success_criteria>
- TRCK-02 satisfied
- RED/GREEN documented in 02-02-SUMMARY.md
</success_criteria>

<output>
Create .planning/phases/02-tracker-announce/02-02-SUMMARY.md
</output>
