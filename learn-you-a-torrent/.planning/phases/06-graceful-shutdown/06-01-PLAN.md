---
phase: 06-graceful-shutdown
plan: 01
type: tdd
wave: 1
depends_on: [05-03]
files_modified:
  - internal/downloader/download.go
  - internal/downloader/download_test.go
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "Download returns context.Canceled when ctx cancelled"
    - "Workers drain and writer closes on cancel"
    - "Output file has no unverified piece data"
---

<objective>
TDD: Downloader graceful cancellation — wait workers, close writer, unblock reads.
</objective>

<tasks>
<task type="tdd" phase="red">
  <name>RED: TestDownloader_cancelledContext</name>
  <files>internal/downloader/download_test.go</files>
  <action>
    Blocking mock TCP peer (handshake + bitfield + unchoke then block on ReadMessage).
    Start Download with cancel after handshake phase.
    Assert errors.Is(err, context.Canceled).
    Assert output file empty or unchanged (0 verified pieces).
    No implementation changes yet — test may fail wrong reason initially.
  </action>
  <verify>
    <automated>go test ./internal/downloader/... -run TestDownloader_cancel -v 2>&1; test $? -ne 0</automated>
  </verify>
</task>
<task type="tdd" phase="green">
  <name>GREEN: Graceful cancel in Download and peerWorker</name>
  <files>internal/downloader/download.go</files>
  <action>
    On ctx.Done: wait wg, writer.Close(), return ctx.Err().
    peerWorker: close conn when ctx cancelled.
  </action>
  <verify>
    <automated>go test ./internal/downloader/... -run TestDownloader_cancel -v</automated>
  </verify>
</task>
</tasks>
