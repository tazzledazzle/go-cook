---
phase: 06-graceful-shutdown
plan: 02
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/torrent/main.go
  - cmd/torrent/main_test.go
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "SIGINT cancels download context"
    - "Shutdown message includes progress snapshot"
---

<objective>
TDD: CLI signal handling with injectable notify hook and shutdown progress message.
</objective>

<tasks>
<task type="tdd" phase="red">
  <name>RED: TestRunDownload_cancelPrintsProgress</name>
  <files>cmd/torrent/main_test.go</files>
  <action>
    Test runDownload with injectable signalNotify that immediately sends os.Interrupt.
    Use stub Downloader or runDownload(ctx) with cancelled context + capture stdout.
    Assert output contains "Shutdown:" and progress fields.
  </action>
  <verify>
    <automated>go test ./cmd/torrent/... -run TestRunDownload_cancel -v 2>&1; test $? -ne 0</automated>
  </verify>
</task>
<task type="tdd" phase="green">
  <name>GREEN: signal.Notify + shutdown message</name>
  <files>cmd/torrent/main.go</files>
  <action>
    runDownload(ctx, args) — signal handler cancels ctx on SIGINT/SIGTERM.
    Track lastProgress from OnProgress; on context.Canceled print Shutdown line.
  </action>
  <verify>
    <automated>go test ./... && go test -race ./...</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Phase 6 ROADMAP criteria 1–4 met
</success_criteria>
