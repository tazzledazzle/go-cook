---
phase: 03-peer-handshake-messages
plan: 03
type: tdd
wave: 3
depends_on: [03-01]
files_modified:
  - internal/peer/message.go
  - internal/peer/message_test.go
autonomous: true
requirements: [PEER-03]
must_haves:
  truths:
    - "ReadMessage parses keepalive (length 0)"
    - "ReadMessage parses choke, unchoke, interested, have, bitfield"
    - "WriteMessage roundtrips through ReadMessage on bytes.Buffer"
---

<objective>
TDD: length-prefixed peer message framing (Pitfall 4).

Purpose: PEER-03 message layer — no TCP yet, bytes.Buffer/Pipe only.
Output: Message struct, ReadMessage, WriteMessage, ID constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/03-peer-handshake-messages/03-RESEARCH.md
@.planning/research/PITFALLS.md (Pitfall 4)
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Message framing table tests</name>
  <files>internal/peer/message_test.go</files>
  <action>
    Table tests Write then Read roundtrip on bytes.Buffer:
    - keepalive (length 0)
    - choke, unchoke, interested (id only, length 1)
    - have with 4-byte piece index payload
    - bitfield with arbitrary payload bytes
    TestReadMessage_incompleteData returns error on truncated stream.
    No message.go — fail compile.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run 'Test(Read|Write)Message' -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement ReadMessage/WriteMessage</name>
  <files>internal/peer/message.go</files>
  <action>
    Message struct { ID uint8; Payload []byte }.
    Constants MsgChoke=0 through MsgCancel=8.
    WriteMessage: 4-byte BE length (1+len(payload)), id, payload.
    Keepalive: write 4 zero bytes only.
    ReadMessage: read 4 length bytes; if 0 return keepalive; else read id + payload (length-1).
    Use io.ReadFull.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run Message -v</automated>
  </verify>
  <done>GREEN verified.</done>
</task>

</tasks>

<output>
Create 03-03-SUMMARY.md
</output>
