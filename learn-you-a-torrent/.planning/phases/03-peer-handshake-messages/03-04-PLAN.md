---
phase: 03-peer-handshake-messages
plan: 04
type: tdd
wave: 4
depends_on: [03-02, 03-03]
files_modified:
  - internal/peer/connection.go
  - internal/peer/connection_test.go
autonomous: true
requirements: [PEER-03]
must_haves:
  truths:
    - "Connection completes handshake then reads bitfield and unchoke from mock peer"
    - "NewConnection over net.Pipe succeeds with mock peer goroutine"
---

<objective>
TDD integration: Connection wraps net.Conn, performs handshake, reads first wire messages.

Purpose: Phase 3 ROADMAP success criteria — connect, handshake, read bitfield/unchoke.
Output: Connection type with Handshake() and ReadMessage() methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/03-peer-handshake-messages/03-02-PLAN.md
@.planning/phases/03-peer-handshake-messages/03-03-PLAN.md
@internal/peer/handshake.go
@internal/peer/message.go
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: TestConnection_readsBitfieldAndUnchoke</name>
  <files>internal/peer/connection_test.go</files>
  <action>
    net.Pipe mock peer goroutine:
    1. Read client handshake from pipe (consume 68 bytes)
    2. Write matching handshake response (same info_hash)
    3. WriteMessage bitfield payload []byte{0xff}
    4. WriteMessage unchoke
    Test: NewConnection(clientConn, expectedHash, ourHandshake).PerformHandshake() then ReadMessage twice → bitfield then unchoke.
    No connection.go — must fail.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run TestConnection -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Connection</name>
  <files>internal/peer/connection.go</files>
  <action>
    Connection struct { conn net.Conn; infoHash [20]byte; handshake Handshake }.
    NewConnection(conn, expectedHash, ours Handshake) *Connection.
    PerformHandshake() calls ExchangeHandshake on conn.
    ReadMessage() delegates to peer.ReadMessage(conn).
  </action>
  <verify>
    <automated>go test ./internal/peer/... -v && go test ./...</automated>
  </verify>
  <done>Phase 3 complete — all PEER reqs satisfied.</done>
</task>

</tasks>

<success_criteria>
- ROADMAP Phase 3 success criteria 1–4 met
- go test ./... green
</success_criteria>

<output>
Create 03-04-SUMMARY.md
</output>
