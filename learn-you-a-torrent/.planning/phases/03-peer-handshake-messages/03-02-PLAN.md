---
phase: 03-peer-handshake-messages
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - internal/peer/handshake.go
  - internal/peer/handshake_test.go
autonomous: true
requirements: [PEER-02]
must_haves:
  truths:
    - "ExchangeHandshake rejects peer info_hash mismatch"
    - "ExchangeHandshake succeeds when peer info_hash matches expected"
---

<objective>
TDD: verify peer's info_hash during handshake exchange over io.ReadWriter.

Purpose: PEER-02 — peers reject wrong hash; we must too.
Output: ExchangeHandshake(rw, expected [20]byte, ours Handshake) error
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/03-peer-handshake-messages/03-01-PLAN.md
@internal/peer/handshake.go
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: ExchangeHandshake match and mismatch tests</name>
  <files>internal/peer/handshake_test.go</files>
  <action>
    Add TestHandshakeExchange_success using net.Pipe:
    - Goroutine writes Serialize(peerHandshakeWithMatchingHash) to server end
    - Client calls ExchangeHandshake(clientConn, expectedHash, ourHandshake)
    - Must return peer's handshake info without error
    Add TestHandshakeExchange_rejectsMismatch:
    - Goroutine writes handshake with wrong info_hash
    - ExchangeHandshake must return error containing "info_hash" or similar
    No ExchangeHandshake implementation yet — must fail.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run TestHandshakeExchange -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement ExchangeHandshake</name>
  <files>internal/peer/handshake.go</files>
  <action>
    Write 68 bytes ours to rw, read 68 bytes back, Deserialize, compare InfoHash to expected.
    Return error if mismatch.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run TestHandshakeExchange -v</automated>
  </verify>
  <done>GREEN verified — PEER-02 complete.</done>
</task>

</tasks>

<output>
Create 03-02-SUMMARY.md
</output>
