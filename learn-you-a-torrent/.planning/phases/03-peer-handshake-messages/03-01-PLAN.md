---
phase: 03-peer-handshake-messages
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/peer/handshake.go
  - internal/peer/handshake_test.go
autonomous: true
requirements: [PEER-01]
must_haves:
  truths:
    - "Handshake.Serialize produces exactly 68 bytes"
    - "Deserialize(Serialize(h)) roundtrips InfoHash and PeerID"
    - "Protocol string and pstrlen=19 validated on deserialize"
---

<objective>
TDD: 68-byte BitTorrent handshake wire format serialize/deserialize.

Purpose: PEER-01 — Pitfall 3 (wrong handshake length/format).
Output: Handshake struct, Serialize, Deserialize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/03-peer-handshake-messages/03-CONTEXT.md
@.planning/phases/03-peer-handshake-messages/03-RESEARCH.md
@.planning/research/PITFALLS.md
@testdata/README.md
</context>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Handshake roundtrip and length tests</name>
  <read_first>
    - .planning/phases/03-peer-handshake-messages/03-RESEARCH.md (handshake table)
    - .planning/research/PITFALLS.md (Pitfall 3)
  </read_first>
  <files>internal/peer/handshake_test.go</files>
  <action>
    Create handshake_test.go ONLY.
    TestHandshake_serializeLength: Serialize returns 68 bytes.
    TestHandshake_roundtrip: known InfoHash (from testdata/README) + test PeerID survive Deserialize(Serialize(h)).
    TestHandshake_deserializeRejectsShortBuffer: 67 bytes → error.
    Do NOT create handshake.go yet.
    Run tests — must fail (undefined Handshake / Serialize).
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run TestHandshake -v 2>&1; test $? -ne 0</automated>
  </verify>
  <done>RED verified.</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Implement Handshake Serialize/Deserialize</name>
  <files>internal/peer/handshake.go</files>
  <action>
    const protocol = "BitTorrent protocol", pstrlen = 19, total = 68.
    Handshake struct { InfoHash, PeerID [20]byte }.
    Serialize writes pstrlen, protocol, 8 zero reserved bytes, info_hash, peer_id.
    Deserialize validates length 68, pstrlen, protocol string, extracts hashes.
  </action>
  <verify>
    <automated>go test ./internal/peer/... -run TestHandshake -v</automated>
  </verify>
  <done>GREEN verified — PEER-01 complete.</done>
</task>

</tasks>

<success_criteria>
- PEER-01 satisfied
- RED/GREEN commits separate
</success_criteria>

<output>
Create 03-01-SUMMARY.md with RED failure message recorded.
</output>
