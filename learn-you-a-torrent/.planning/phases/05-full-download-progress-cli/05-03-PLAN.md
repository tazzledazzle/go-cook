---
phase: 05-full-download-progress-cli
plan: 03
type: tdd
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - internal/torrent/download.go
  - internal/torrent/download_test.go
autonomous: true
requirements: [CLI-01]
must_haves:
  truths:
    - "Downloader completes minimal.torrent via mock tracker + TCP mock peer"
    - "Output file contains hello world"
---

<objective>
TDD: Downloader orchestrator — announce, dial one peer, download all pieces, close writer.
</objective>

<tasks>
<task type="tdd" phase="red">
  <name>RED: TestDownloader_downloadsMinimalTorrent</name>
  <files>internal/torrent/download_test.go</files>
  <action>
    TCP listener mock peer (Phase 4 handler logic).
    httptest tracker returns compact peer for listener port.
    Downloader.Download(ctx, tor, tempDir) → ReadFile test.txt == "hello world\n".
    No download.go — fail compile.
  </action>
  <verify>
    <automated>go test ./internal/torrent/... -run TestDownloader_downloadsMinimal -v 2>&1; test $? -ne 0</automated>
  </verify>
</task>
<task type="tdd" phase="green">
  <name>GREEN: Implement Downloader</name>
  <files>internal/torrent/download.go</files>
  <action>
    Downloader{Tracker, PeerID, OnProgress func(Progress)}.
    Download(ctx, tor, dir): announce, NewWriter, Manager, single peerWorker for v1 test path.
    peerWorker: dial, handshake, loop NextMissing→DownloadPiece→MarkComplete→OnProgress.
    Close writer when all pieces complete.
  </action>
  <verify>
    <automated>go test ./internal/torrent/... -run TestDownloader_downloadsMinimal -v</automated>
  </verify>
</task>
</tasks>
