---
phase: 05-full-download-progress-cli
plan: 05
type: tdd
wave: 5
depends_on: [05-03, 05-04]
files_modified:
  - cmd/torrent/main.go
  - cmd/torrent/main_test.go
  - README.md
autonomous: true
requirements: [CLI-01, CLI-02, TEST-03]
must_haves:
  truths:
    - "torrent download subcommand parses .torrent and exits 0 on success"
    - "README documents manual E2E verification steps"
---

<objective>
TDD: CLI entrypoint + manual verification documentation.
</objective>

<tasks>
<task type="tdd" phase="red">
  <name>RED: CLI download command test</name>
  <files>cmd/torrent/main_test.go</files>
  <action>
    TestRunDownload_invalidArgs: missing args → error.
    TestRunDownload_success: inject/mock Downloader or use test helper Run([]string{"download", path}) with stub — prefer testing runDownload function directly with temp dir if network stubbed.
    Minimal: test parseArgs + runDownload returns error for bad path; integration via TestMain helper.
    No main.go — fail compile.
  </action>
  <verify>
    <automated>go test ./cmd/torrent/... -run TestRun -v 2>&1; test $? -ne 0</automated>
  </verify>
</task>
<task type="tdd" phase="green">
  <name>GREEN: CLI main + README TEST-03</name>
  <files>cmd/torrent/main.go, README.md</files>
  <action>
    main: `torrent download <file.torrent>` → ParseTorrent → Downloader.Download → print progress lines.
    README: Manual Verification section with minimal.torrent steps and optional public ISO note.
  </action>
  <verify>
    <automated>go test ./... && go test -race ./...</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Phase 5 ROADMAP criteria 1–4 met
- go test ./... green
</success_criteria>
