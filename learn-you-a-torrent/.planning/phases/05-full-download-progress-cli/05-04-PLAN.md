---
phase: 05-full-download-progress-cli
plan: 04
type: tdd
wave: 4
depends_on: [05-03]
files_modified:
  - internal/torrent/download.go
  - internal/torrent/download_test.go
autonomous: true
requirements: [CLI-01]
must_haves:
  truths:
    - "Downloader spawns goroutine per tracker peer"
    - "Progress reports active peer count > 1 when tracker returns 2 peers"
---

<objective>
TDD: concurrent peer workers — one goroutine per tracker peer address.
</objective>

<tasks>
<task type="tdd" phase="red">
  <name>RED: TestDownloader_multiplePeers</name>
  <files>internal/torrent/download_test.go</files>
  <action>
    Two TCP listeners (or one listener accepting 2 conns) with identical mock peer handlers.
    Tracker returns 2 compact peers.
    Capture max ActivePeers from OnProgress callback; assert >= 2.
    Download still completes with correct file.
  </action>
  <verify>
    <automated>go test ./internal/torrent/... -run TestDownloader_multiplePeers -v 2>&1; test $? -ne 0</automated>
  </verify>
</task>
<task type="tdd" phase="green">
  <name>GREEN: Multi-peer worker pool</name>
  <files>internal/torrent/download.go</files>
  <action>
    Download spawns go peerWorker for each announced peer.
    sync.WaitGroup or channel waits for all workers; first error cancels ctx.
    Atomic/mutex active peer counter for Progress.ActivePeers.
  </action>
  <verify>
    <automated>go test ./internal/torrent/... -run TestDownloader -v && go test -race ./internal/torrent/...</automated>
  </verify>
</task>
</tasks>
