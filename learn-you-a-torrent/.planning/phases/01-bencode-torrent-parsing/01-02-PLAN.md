---
phase: 01-bencode-torrent-parsing
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - internal/bencode/decode.go
  - internal/bencode/decode_test.go
autonomous: true
requirements: [BENC-01, BENC-02]
must_haves:
  truths:
    - "Decoder parses bencode strings, integers, lists, and dictionaries"
    - "Decoder captures raw info dictionary byte slice during top-level decode without re-encoding"
    - "Table-driven tests cover all four bencode types plus nested structures"
  artifacts:
    - path: "internal/bencode/decode.go"
      provides: "Full bencode decoder with InfoDictBytes() accessor"
    - path: "internal/bencode/decode_test.go"
      provides: "Table-driven tests for all types and info offset capture"
  key_links:
    - from: "internal/bencode/decode.go"
      to: "testdata/minimal.torrent"
      via: "InfoDictBytes returns exact info dict slice for SHA1"
      pattern: "InfoDictBytes"
---

<objective>
Implement a complete bencode decoder with info-dictionary byte capture for correct info hash calculation.

Purpose: Satisfy BENC-01 (full decode) and BENC-02 (raw info dict extraction).
Output: Production-quality internal/bencode package with comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-bencode-torrent-parsing/01-RESEARCH.md
@.planning/phases/01-bencode-torrent-parsing/01-01-PLAN.md
@.planning/research/PITFALLS.md
@internal/bencode/decode.go
@internal/bencode/decode_test.go
@testdata/minimal.torrent
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Table-driven tests for all bencode types</name>
  <read_first>
    - internal/bencode/decode.go
    - internal/bencode/decode_test.go
    - .planning/phases/01-bencode-torrent-parsing/01-RESEARCH.md (bencode format table)
  </read_first>
  <files>internal/bencode/decode_test.go</files>
  <behavior>
    - String: 4:spam → spam
    - Int: i42e → 42, i-3e → -3
    - List: li1ei2ei3ee → [1,2,3]
    - Dict: d3:foo3:bare → map[foo:bar]
    - Nested: ldi1e4:spame → list containing dict
    - Invalid input returns error (not panic)
  </behavior>
  <action>
    Expand decode_test.go with table-driven TestDecode covering at minimum:
    4:spam, i42e, i-3e, li1ei2ei3ee, d3:foo3:bare, ldi1e4:spame.
    Add TestDecode_invalid with cases: empty input, i42 (missing e), 5:abc (length mismatch).
    Run tests — expect failures until Task 2 implements decode.
  </action>
  <acceptance_criteria>
    - decode_test.go has TestDecode with at least 6 passing type cases after Task 2
    - decode_test.go has TestDecode_invalid with at least 2 error cases
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/bencode/... -v -run TestDecode</automated>
  </verify>
  <done>All bencode type tests defined and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement full decoder with info dict byte capture</name>
  <read_first>
    - internal/bencode/decode.go
    - .planning/research/PITFALLS.md (Pitfall 1)
    - testdata/minimal.torrent
    - testdata/README.md (info dict offset reference if documented)
  </read_first>
  <files>internal/bencode/decode.go</files>
  <action>
    Implement decodeInt, decodeList, decodeDict on Decoder.
    decodeDict: when key string equals info at top level (track nesting depth — only capture at depth 0), set infoStart = pos before decoding value dict, infoEnd = pos after value consumed.
    Add method InfoDictBytes() []byte returning data[infoStart:infoEnd] or nil if not found.
    Add method Pos() int for test assertions.
    Decode top-level .torrent file in TestInfoDictBytes: read testdata/minimal.torrent, decode, assert InfoDictBytes()[0]=='d' and last byte=='e', assert len matches README documented length.
  </action>
  <acceptance_criteria>
    - go test ./internal/bencode/... exits 0
    - TestInfoDictBytes passes: captured slice equals manual subslice of file bytes at documented offsets
    - InfoDictBytes does NOT re-encode — test verifies byte-for-byte match with os.ReadFile subslice
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/bencode/... -v</automated>
  </verify>
  <done>BENC-01 and BENC-02 satisfied with full decoder and offset capture.</done>
</task>

</tasks>

<verification>
- go test ./internal/bencode/... -v
- go test ./internal/bencode/... -run TestInfoDictBytes -v
</verification>

<success_criteria>
- All four bencode types decode correctly (BENC-01)
- Raw info dict bytes extracted without re-encoding (BENC-02)
</success_criteria>

<output>
Create .planning/phases/01-bencode-torrent-parsing/01-02-SUMMARY.md when done.
</output>
