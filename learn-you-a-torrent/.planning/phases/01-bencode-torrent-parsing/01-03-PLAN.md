---
phase: 01-bencode-torrent-parsing
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - internal/torrent/torrent.go
  - internal/torrent/info.go
  - internal/torrent/parser.go
  - internal/torrent/info_hash.go
  - internal/torrent/parser_test.go
autonomous: true
requirements: [TORR-01, TORR-02]
must_haves:
  truths:
    - "Parser reads testdata/minimal.torrent and populates Torrent struct with announce, name, piece length, piece count"
    - "InfoHash equals golden hash from testdata/README.md — SHA1 of raw info dict bytes"
    - "Golden test fails if info dict is re-encoded instead of sliced from file"
  artifacts:
    - path: "internal/torrent/parser.go"
      provides: "Open and ParseTorrent from file path or bytes"
    - path: "internal/torrent/info_hash.go"
      provides: "InfoHash type and SHA1 from raw bytes"
    - path: "internal/torrent/parser_test.go"
      provides: "Golden tests for minimal.torrent"
  key_links:
    - from: "internal/torrent/parser.go"
      to: "internal/bencode/decode.go"
      via: "Decoder.InfoDictBytes for hash; decoded map for struct fields"
      pattern: "bencode.NewDecoder"
    - from: "internal/torrent/info_hash.go"
      to: "crypto/sha1"
      via: "sha1.Sum(rawInfoBytes)"
      pattern: "sha1.Sum"
---

<objective>
Parse a .torrent file into typed structs and compute the correct 20-byte info hash from raw info dictionary bytes.

Purpose: Satisfy TORR-01 (parse metadata) and TORR-02 (correct info hash). Completes Phase 1 goal.
Output: internal/torrent package with golden-file tests against testdata/minimal.torrent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-bencode-torrent-parsing/01-RESEARCH.md
@.planning/phases/01-bencode-torrent-parsing/01-02-PLAN.md
@.planning/research/PITFALLS.md
@internal/bencode/decode.go
@testdata/minimal.torrent
@testdata/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Torrent/Info types and golden parser tests</name>
  <read_first>
    - .planning/phases/01-bencode-torrent-parsing/01-RESEARCH.md (torrent structure)
    - testdata/README.md
    - testdata/minimal.torrent
    - internal/bencode/decode.go
  </read_first>
  <files>internal/torrent/torrent.go, internal/torrent/info.go, internal/torrent/parser_test.go</files>
  <action>
    Define Torrent struct: Announce string, Info Info, InfoHash [20]byte.
    Define Info struct: Name string, PieceLength int64, Length int64, Pieces []byte (raw 20-byte hash concatenation).
    Add helper PieceCount() int returning len(Pieces)/20.
    Write parser_test.go:
    TestParseMinimalTorrent — ParseTorrent(testdata/minimal.torrent), assert Name=test.txt, PieceLength=16384, Length=12, PieceCount=1, Announce contains example.com.
    TestInfoHashGolden — assert InfoHash hex matches testdata/README.md Expected info_hash value.
    Tests fail until Task 2 implements parser.
  </action>
  <acceptance_criteria>
    - parser_test.go contains TestParseMinimalTorrent and TestInfoHashGolden
    - Golden hash constant matches testdata/README.md exactly (40 hex chars)
    - go test ./internal/torrent/... fails before parser implementation (red phase)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/torrent/... -v -run TestParse 2>&1 | head -20</automated>
  </verify>
  <done>Types and golden tests written; ready for parser implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parser and info hash from raw bytes</name>
  <read_first>
    - internal/torrent/parser_test.go
    - internal/bencode/decode.go
    - .planning/research/PITFALLS.md (Pitfall 1 — do NOT re-encode)
  </read_first>
  <files>internal/torrent/parser.go, internal/torrent/info_hash.go</files>
  <action>
    Implement ParseTorrent(path string) (*Torrent, error):
    Read file bytes, create bencode.Decoder, Decode top-level dict.
    Extract announce string from top-level map key announce.
    Call decoder.InfoDictBytes(), pass to InfoHashFromBytes(raw) using crypto/sha1.
    Parse info fields from decoded map key info: name, piece length (key piece length with space), length, pieces.
    Implement Open(path) as alias if useful.
    Add TestInfoHash_notReencoded regression: tamper test that hashing a re-marshaled info map would differ from golden (document why raw slice matters).
  </action>
  <acceptance_criteria>
    - go test ./internal/torrent/... exits 0
    - TestParseMinimalTorrent passes all field assertions
    - TestInfoHashGolden passes — InfoHash matches README golden hex
    - TestInfoHash_notReencoded passes or documents raw-bytes requirement in test comment
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/torrent/... -v</automated>
  </verify>
  <done>TORR-01 and TORR-02 complete; Phase 1 vertical slice proven.</done>
</task>

</tasks>

<verification>
- go test ./internal/bencode/... ./internal/torrent/... -v
- go test ./... 
</verification>

<success_criteria>
- Parse testdata/minimal.torrent with correct metadata fields (TORR-01)
- Info hash matches golden value from raw info dict (TORR-02)
- All Phase 1 ROADMAP success criteria met
</success_criteria>

<output>
Create .planning/phases/01-bencode-torrent-parsing/01-03-SUMMARY.md when done.
</output>
